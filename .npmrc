# Flat node_modules so the shared toolchain (tsc, vitest, eslint) resolves from
# every workspace package without re-declaring it in each package.json.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
